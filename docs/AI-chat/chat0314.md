# question1
为ch1-clock实现时钟中断，需要如何实现？
要求：
1.基于现在ch1-clock的代码给出修改建议。
2.是否需要实现trap上下文与系统调用。

# res1
AI给出了新的实现方法，我对一些步骤还不清楚，继续提问。

# question2
在实现时钟中断的时候，为什么要设置 stvec CSR 指向 trap handler，在发生中断的时候，系统是如何工作的？
要求：
1.给出操作系统里关于中断处理过程的理论知识
2.给出具体实践中，用rust写的系统，处理中断的流程
3.对比理论知识和实践的差别，以及为什么这么做

# question3
我已经完成编码，但是运行后，一直没有时钟中断的输出，请为我生成一份代码调试计划，并保存在/home/hdu/study/rust/2026s-ai4ose-lab/tg-rcore-tutorial-ch1-clock/docs/ch1-clock-debug01.md里。
要求：
1.给出调试步骤的原因，为什么要这么做
2.给出代码出bug的3-5个可能的原因及对应方法

# res3
根据计划debug
步骤 1: 验证中断是否触发（添加调试输出）没有看到'.'在输出
步骤 2: 检查 sstatus 和 sie CSR------一直在输出空，判断寄存器的值出问题
步骤 3: 检查 scause 是否被设置--------没有输出，判断scause一直为0
步骤 4: 检查 M-mode 中断委托配置
在loop里输入
        let medeleg: usize;
        let mideleg: usize;
        unsafe {
            core::arch::asm!("csrr {0}, medeleg", out(reg) medeleg);
            core::arch::asm!("csrr {0}, mideleg", out(reg) mideleg);
        }
        if medeleg & 0x10 == 0{
            console_putchar(b'1');
        }
        if mideleg & 0x1 == 0{
            console_putchar(b'2');
        }
后，打印了x，并且直接退出，不知道为什么突然进入了trap_handler，并且输出x直接退出，是除了Trap::Interrupt(Interrupt::SupervisorTimer)时钟中断类型的其他类型，但是没有输出
               panic!(
                "Unsupported trap {:?}, stval = {:#x}!",
                scause.cause(),
                stval
                );

步骤 5: 检查 sscratch 初始化--------在init里设置sscratch为0，依然没有输出b，判断不是初始化的问题

# question4
根据上述debug的现象，给出下一步debug计划，并保存在/home/hdu/study/rust/2026s-ai4ose-lab/tg-rcore-tutorial-ch1-clock/docs/ch1-clock-debug02.md里。

# res4
在 RISC-V 特权架构中：
- `medeleg` 和 `mideleg` 是 **M-mode CSR**
- **S-mode 没有权限读取这些寄存器**
- 执行 `csrr` 读取这些寄存器会触发 **非法指令异常 (Illegal Instruction)** 或 **访问异常**

这就是为什么：
1. 添加这段代码后触发了 trap → 进入 trap_handler → 输出 'x'
2. scause 不是 SupervisorTimer（是异常，不是中断）
3. 所以没有匹配到 `Trap::Interrupt(Interrupt::SupervisorTimer)` 分支
4. 应该走到 `_ =>` 分支，输出 'p' 并 panic

# question5
问题是删除访问`medeleg` 和 `mideleg`的代码后，根本进不了中断处理程序，不过这也说明了中断处理程序入口设置的没问题，所以现在的关键是判断为什么时钟中断没有触发？因为如果触发了一定会跳转。给出debug计划并保存在/home/hdu/study/rust/2026s-ai4ose-lab/tg-rcore-tutorial-ch1-clock/docs/ch1-clock-debug03.md里。

# question6
sstatus.SIE 未启用（全局中断开关）------启用后依然没有时钟中断触发
步骤 4: 验证 M-mode 委托------tg-rcore-tutorial-sbi/src/msbi.rs里没有csrw mideleg, 0x22  // 委托 STIP 和 SSIP，我该如何实现，我是否应该直接在msbi里添加trap相关代码，不用自己写trap。
给出实现计划，并保存在/home/hdu/study/rust/2026s-ai4ose-lab/tg-rcore-tutorial-ch1-clock/docs/ch1-clock-debug04.md里。

# question7
问题是现在都没有输出'x'，说明没有进入trap_handler。要求完成
要求：
1.为什么没有进入trap_handler？
2.是因为进入msbi的m_trap_handler了吗？
3.如果是这样给出没有进入trap_handler却进入了m_trap_handler的原因？
4.如果是以上原因，给出解决方案
5.如果不是以上原因，给出3-5个可能的原因，及对应的解决方法
6.保存在/home/hdu/study/rust/2026s-ai4ose-lab/tg-rcore-tutorial-ch1-clock/docs/ch1-clock-debug05.md里。